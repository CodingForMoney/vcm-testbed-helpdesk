{
  "name": "@vcm-testbed/api",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "node --import tsx --watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node --import tsx src/index.ts"
  },
  "dependencies": {
    "@vcm-testbed/db": "file:../../packages/db",
    "@vcm-testbed/domain": "file:../../packages/domain"
  }
}
